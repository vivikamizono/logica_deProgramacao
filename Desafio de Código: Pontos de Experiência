// Entrada de dados
let num1 = parseInt(gets());
let num2 = parseInt(gets());

// Lógica para calcular a quantidade de XP ganho
const xpGanho = num1 * num2 * 100;

// Imprime a quantidade de XP ganho
print("Voce ganhou " + xpGanho + " XP!");
